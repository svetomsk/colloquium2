<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content">

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/candidate_item"
        android:text="@string/hello_world"
        android:padding="7dp"
        android:layout_margin="3dp"/>
    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="right"
        android:layout_below="@+id/candidate_item"
        android:id="@+id/edit_bar"
        android:visibility="gone">
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/edit_candidate"
            android:layout_toRightOf="@+id/delete_candidate"
            android:text="@string/edit"
            android:onClick="onChangeCandidate"/>
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/delete_candidate"
            android:text="@string/delete"
            android:onClick="onDeleteAction"
        />
    </RelativeLayout>
</RelativeLayout>
